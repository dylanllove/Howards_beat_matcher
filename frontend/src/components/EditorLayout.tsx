import type { Cut } from '../types';
import { formatTime } from '../utils/time';
import PlaybackControls from './PlaybackControls';
import PreviewPanel from './PreviewPanel';
import WaveformTimeline from './WaveformTimeline';

type Props = { duration: number; peaks: number[]; cuts: Cut[]; selectedCutId: string | null; currentTime: number; isPlaying: boolean; zoom: number; flashing: boolean; loopEnabled: boolean; loopWindow: number; onZoom: (z: number) => void; onSeek: (t: number) => void; onTogglePlay: () => void; onAddCut: (t: number) => void; onMoveCut: (id: string, t: number) => void; onSelectCut: (id: string | null) => void; onRemoveCut: (id: string | null) => void; onLoopEnabled: (v: boolean) => void; onLoopWindow: (v: number) => void };
export default function EditorLayout(p: Props) { const selected = p.cuts.find((cut) => cut.id === p.selectedCutId); return <main className="flex min-h-0 flex-1 flex-col gap-3 bg-premiere-950 p-4"><div className="grid grid-cols-4 gap-3 text-sm"><div className="rounded bg-premiere-900 p-3">Duration: <span className="font-mono text-white">{formatTime(p.duration)}</span></div><div className="rounded bg-premiere-900 p-3">Cuts: <span className="font-mono text-white">{p.cuts.length}</span></div><div className="rounded bg-premiere-900 p-3">Zoom: <span className="font-mono text-white">{p.zoom.toFixed(1)}x</span></div><div className="rounded bg-premiere-900 p-3">Selected cut: <span className="font-mono text-white">{selected ? formatTime(selected.time) : '—'}</span></div></div><PreviewPanel flashing={p.flashing}/><WaveformTimeline peaks={p.peaks} duration={p.duration} cuts={p.cuts} selectedCutId={p.selectedCutId} currentTime={p.currentTime} zoom={p.zoom} onZoom={p.onZoom} onSeek={p.onSeek} onAddCut={p.onAddCut} onMoveCut={p.onMoveCut} onSelectCut={p.onSelectCut} onRemoveCut={p.onRemoveCut} isPlaying={p.isPlaying}/><PlaybackControls currentTime={p.currentTime} isPlaying={p.isPlaying} loopEnabled={p.loopEnabled} loopWindow={p.loopWindow} onToggle={p.onTogglePlay} onLoopEnabled={p.onLoopEnabled} onLoopWindow={p.onLoopWindow}/></main>; }
