import { useState } from 'react';
type Props = { onImport: (auto: boolean) => void; onImportPair: () => void; analysing: boolean };
export default function UploadScreen({ onImport, onImportPair, analysing }: Props) { const [auto, setAuto] = useState(true); return <main className="flex h-full items-center justify-center bg-premiere-950"><section className="w-full max-w-xl rounded-xl border border-premiere-600 bg-premiere-900 p-8 shadow-2xl"><p className="mb-2 text-sm uppercase tracking-[0.3em] text-howards">Local audio timing tool</p><h1 className="mb-4 text-4xl font-bold text-white">Howards Beat Matcher</h1><p className="mb-6 text-slate-300">Import an MP3, inspect the waveform, place cuts manually, and export the exact timing_plan JSON used by Howards.</p><label className="mb-6 flex items-center gap-3 rounded bg-premiere-800 p-4"><input type="checkbox" checked={auto} onChange={(e) => setAuto(e.target.checked)} className="h-5 w-5 accent-howards"/> Auto-generate suggested cuts immediately</label><div className="flex gap-3"><button onClick={() => onImport(auto)} disabled={analysing} className="rounded bg-howards px-5 py-3 font-semibold text-black">Choose MP3</button><button onClick={onImportPair} disabled={analysing} className="rounded border border-premiere-600 px-5 py-3">Choose MP3 + JSON</button></div><p className="mt-5 text-xs text-slate-500">Export folders require Chrome or Edge for the File System Access API.</p></section></main>; }
