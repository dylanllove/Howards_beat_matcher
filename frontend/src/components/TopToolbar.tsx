import type { ReactNode } from 'react';
import { Download, FileAudio, FolderOpen, Plus, RotateCcw, RotateCw, Sparkles } from 'lucide-react';

type Props = { hasProject: boolean; analysing: boolean; canUndo: boolean; canRedo: boolean; onNew: () => void; onImportMp3: () => void; onImportPair: () => void; onAutoAdd: () => void; onUndo: () => void; onRedo: () => void; onExport: () => void };
const Btn = ({ children, onClick, disabled }: { children: ReactNode; onClick: () => void; disabled?: boolean }) => <button onClick={onClick} disabled={disabled} className="flex items-center gap-2 rounded border border-premiere-600 bg-premiere-800 px-3 py-2 text-sm hover:border-howards hover:text-white">{children}</button>;
export default function TopToolbar(p: Props) { return <div className="flex flex-wrap items-center gap-2 border-b border-premiere-700 bg-premiere-900 p-3 shadow"><div className="mr-4 text-lg font-semibold text-white">Howards <span className="text-howards">Beat Matcher</span></div><Btn onClick={p.onNew}><Plus size={16}/>New Project</Btn><Btn onClick={p.onImportMp3}><FileAudio size={16}/>Import MP3</Btn><Btn onClick={p.onImportPair}><FolderOpen size={16}/>Import MP3 + JSON</Btn><Btn onClick={p.onAutoAdd} disabled={!p.hasProject || p.analysing}><Sparkles size={16}/>{p.analysing ? 'Analysing…' : 'Auto Add Suggested Cuts'}</Btn><Btn onClick={p.onUndo} disabled={!p.canUndo}><RotateCcw size={16}/>Undo</Btn><Btn onClick={p.onRedo} disabled={!p.canRedo}><RotateCw size={16}/>Redo</Btn><button onClick={p.onExport} disabled={!p.hasProject} className="ml-auto flex items-center gap-2 rounded bg-howards px-4 py-2 text-sm font-semibold text-black hover:bg-emerald-400"><Download size={16}/>Export</button></div>; }
