import { Pause, Play } from 'lucide-react';
import { formatTime } from '../utils/time';
type Props = { currentTime: number; isPlaying: boolean; loopEnabled: boolean; loopWindow: number; onToggle: () => void; onLoopEnabled: (v: boolean) => void; onLoopWindow: (v: number) => void };
export default function PlaybackControls(p: Props) { return <div className="flex items-center gap-4 border-t border-premiere-700 bg-premiere-900 px-4 py-3"><button onClick={p.onToggle} className="rounded-full bg-howards p-3 text-black">{p.isPlaying ? <Pause size={18}/> : <Play size={18}/>}</button><span className="font-mono text-sm text-slate-200">{formatTime(p.currentTime)}</span><label className="ml-auto flex items-center gap-2 text-sm"><input type="checkbox" checked={p.loopEnabled} onChange={(e) => p.onLoopEnabled(e.target.checked)} className="accent-howards"/> Loop selected cut</label><select value={p.loopWindow} onChange={(e) => p.onLoopWindow(Number(e.target.value))} className="rounded bg-premiere-800 px-2 py-1 text-sm"><option value={0.5}>0.5s before/after</option><option value={1}>1s before/after</option><option value={2}>2s before/after</option></select></div>; }
