export default function PreviewPanel({ flashing }: { flashing: boolean }) { return <section className={`flex min-h-64 flex-1 items-center justify-center rounded border border-premiere-700 transition-colors duration-150 ${flashing ? 'bg-howards' : 'bg-black'}`}><div className="text-center"><div className="text-sm uppercase tracking-[0.4em] text-slate-500">Blank Preview Panel</div><div className="mt-3 text-xs text-slate-600">No video is shown in this audio timing tool</div></div></section>; }
